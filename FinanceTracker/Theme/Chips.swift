import SwiftUI

extension Theme {
    enum Chips {
        static let suggestionBackground: Color = Theme.Colors.primaryInteractive.opacity(0.12)

        static let confidenceHigh:   Double = 1.0
        static let confidenceMedium: Double = 0.7
        static let confidenceLow:    Double = 0.4
    }
}
