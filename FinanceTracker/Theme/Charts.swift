import SwiftUI

extension Theme {
    enum Charts {
        static let balanceLine:      Color   = .teal
        static let balanceAreaFill:  Color   = .teal.opacity(0.08)
        static let spendingBar:      Color   = .orange
        static let gridLine:         Color   = Color(.separator)

        static let minHeight:        CGFloat = 180
        static let lineStrokeWidth:  CGFloat = 2
    }
}
