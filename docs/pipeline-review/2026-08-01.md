---
date: 2026-08-01
addressed: false
---

# Pipeline Review — 2026-08-01

## Critical
- [x] `/goal` is referenced as an invokable command in three command files but does not exist in the current skill registry or anywhere on disk (`~/.claude/skills/`, `.claude/`) — it appears only in doc prose, never implemented. Locations: `.claude/commands/feature.md:56`, `.claude/commands/gates.md:175`, `.claude/commands/test.md:67`. Traced via `git log --all --grep=goal`: added in PR #49/#50 ("tighten /loop and /goal autonomous-run guidance") as documentation only — no `goal.md` was ever created in either repo. — **Fix:** either build a `/goal` command/skill (goal-condition-driven autonomous loop, distinct from interval-based `/loop`) or replace all three references with the equivalent `/loop` phrasing, since `/loop` is the only registered autonomous-run primitive today. — **Resolved:** replaced all three with `/loop <task>. Stop when <condition>.` phrasing; also corrected stale "8 gates" counts to 9 in the two `feature.md` lines touched along the way.
- [x] `.claude/commands/sync-workflow.md:45` runs `gh pr create --repo akshaypimprikar/pragma` with **no `--head` flag** — this is the exact gap CLAUDE.md's Cross-repo rule was just rewritten today to close, and it already caused a real incident: PR #76's own body states "its absence caused a wrong-head PR against pragma today (closed pragma#32, recreated as pragma#34)." The prose rule in `CLAUDE.md:49` was fixed; the command file that actually performs this operation was not. Next time `/sync-workflow` runs, it will reproduce the same bug. — **Fix:** add `--head sync/<YYYY-MM-DD>` to the `gh pr create` call in `sync-workflow.md:45`, matching CLAUDE.md's Cross-repo rule, and add the `gh pr view --json headRefName,baseRefName,files` verification step CLAUDE.md now recommends. — **Resolved.**

## High
- [x] `.claude/commands/feature.md:48` and `.claude/commands/plan.md:53` both still state "`/test` and `code-review:code-review` run in parallel" — PR #73 (2026-07-30) already established this is impossible (`code-review:code-review` has `disable-model-invocation` set and isn't agent-invocable at all) and corrected the same claim in `parallel-review.md` and `pr-followup.md`. The fix was not back-ported to `feature.md`/`plan.md`, so two of the four places this workflow is documented still describe a mechanism that cannot happen. — **Fix:** update both lines to match `pr-followup.md`'s accurate wording (review → test sequential; code-review manual, not parallel). — **Resolved.**
- [x] `pragma/.claude/commands/gates.md` has an internal gate-count inconsistency: the file defines 9 numbered gates (Build, Tests, TODO, Branch, CHANGELOG, Coverage, Security, Abstraction bloat, Architecture) but the "Autonomous gate-fixing loop" and "Done when" sections say "8 gates" (lines 177, 224) and "7 gates" (line 183) — three different wrong numbers in the same file. This predates today's release cycle but was not caught by prior syncs. — **Fix:** update all three references in `pragma/.claude/commands/gates.md` to "9 gates," consistent with the 9 numbered gate sections actually present. — **Resolved** via pragma PR (bundled with the `parallel-review.md` port below).

## Medium
- [x] `parallel-review.md` exists in `.claude/commands/` (FinanceTracker) with no counterpart in `pragma/.claude/commands/` — it has never been synced to the template despite being a generalizable pre-PR checkpoint pattern (not FinanceTracker-specific logic). — **Fix:** run `/sync-workflow` to port a templated `<AppName>`-placeholder version of `parallel-review.md` to pragma. — **Resolved** via pragma PR.
- [x] `.claude/commands/design.md` has no section literally titled `## Process` — it has `## Bootstrap process` and `## Extend process` instead, so it fails the Trigger/Process/Done-when completeness check even though the content is functionally present. — **Fix:** add a top-level `## Process` header (or rename one of the two subsections) so the file satisfies the standard section contract other command files use. — **Resolved:** added `## Process` header, demoted `Bootstrap process`/`Extend process` to `###` and their numbered steps to `####` to keep the heading hierarchy consistent.
- [x] Memory `project_pipeline_followups_pending.md` states "pragma PR #31 open, not yet merged" (and "If pragma PR #31 hasn't merged next session, that's the next mechanical step"). `gh pr list --repo akshaypimprikar/pragma` confirms PR #31 was merged 2026-07-30. — **Fix:** update the memory to reflect PR #31 as merged, or retire the file since both threads it tracks are now fully resolved with no open mechanical step remaining. — **Resolved:** retired.
- [x] Memory `project_skills_to_introduce.md` states "fewer-permission-prompts still outstanding" / "still pending." The skill is registered and available (`fewer-permission-prompts` appears in the current skill list), and PR #76's body attributes its own settings.json additions to "a `fewer-permission-prompts` pass earlier this session" — i.e. it has already been run at least once this cycle. — **Fix:** update the memory to reflect that the skill exists and has been used at least once; re-scope the "outstanding" framing to the still-unbuilt extension (auto-prune on PR-create/session-end) described later in the same file, which is the part that's actually still open. — **Resolved:** updated.
- [x] Memory `feedback_gh_pr_base_develop.md` describes a failure mode ("omitting `--base` silently targets main, bypassed gitflow on PR #15") that is now fully codified as CLAUDE.md's own enforced "PR creation rule" (`CLAUDE.md:46`) — CLAUDE.md is the current source of truth for this rule, not the memory. — **Fix:** retire the memory file (fold the PR #15 historical note into CLAUDE.md's rule as a one-line rationale if worth keeping) per the same redundancy criterion `/trim-context` already applies to memories restating settled CLAUDE.md content. — **Resolved:** retired.

## Low
- [x] `.claude/settings.json` grants `Read(//Users/akshaypimprikar/**)` — unrestricted recursive read of the entire home directory, broader than any task in this pipeline needs. — **Fix:** narrow to the specific subtrees actually read (e.g. `Desktop/Claude/**`, `.claude/**`) and drop the blanket grant. — **Resolved:** narrowed to `Desktop/**` and `Documents/**`.
- [ ] `.claude/settings.json` grants `Bash(python3 -c ' *)` and `Bash(python3 -)` — both allow arbitrary, unreviewed Python execution (inline `-c` string or piped stdin) with no pattern constraint beyond the leading flag. — **Fix:** replace with allowlisted entries for the specific one-off scripts already run (or move them to `.claude/settings.local.json` per the existing session-noise convention in `project_skills_to_introduce.md`) instead of a standing arbitrary-exec grant. — **Not resolved:** unlike the Read narrowing, there's no safe default here — removing it risks breaking frequent legitimate one-liner usage, and it's a real convenience/security tradeoff. Left for the user to decide.
- [x] `CLAUDE.md:40`'s "Agent commands" line lists 12 commands (`/spec /plan /feature /gates /test /review /pr-followup /bugfix /release /sync-workflow /design /pipeline-review`) but `.claude/commands/` contains 15 files — `/status`, `/parallel-review`, and `/trim-context` exist but aren't mentioned, which could read as those commands not existing. — **Fix:** add the three missing names to the list, or explicitly scope the line to "standard pipeline commands" if the omission of meta/support commands is intentional. — **Resolved.**
- [x] `/release`'s pre-flight (`release.md:12-29`) checks tests/TODO/force-unwraps but has no explicit guard confirming a release branch touches only `project.pbxproj`/`CHANGELOG.md` — the Merge rule's exemption of `release/*` PRs from `/review` + `code-review:code-review` relies entirely on the informal assumption that release branches never carry new logic. — **Fix:** add a pre-flight check (e.g. `git diff develop...HEAD --name-only` asserting the changed-file set is a subset of `{project.pbxproj, CHANGELOG.md, .claude/context/feature-log.md}`) so the exemption's safety is enforced, not just assumed. — **Resolved:** added as new step 5 in `release.md`'s Process (before opening the PR), subsequent steps renumbered.

## All clear
- CLAUDE.md token budget: 49 lines, within the ≤50 target
- Pipeline gate coverage: all 7 required gates/steps present (`/gates`, Gate 1 build, `/feature` CHANGELOG step, review-before-test ordering, Gate 6 coverage, Gate 7 security, `/status`)
- Settings hygiene — hooks: both `PostToolUse` and `UserPromptSubmit` hooks are inline commands referencing no external scripts; nothing missing. `UserPromptSubmit`'s pipeline-review-alert hook is present and correctly targets `docs/pipeline-review/*.md`
- Template drift — app-specific leakage: `gates.md`, `feature.md`, `review.md`, `pr-followup.md`, `plan.md`, `bugfix.md`, `spec.md`, `status.md`, `sync-workflow.md`, `trim-context.md`, `pipeline-review.md`, `design.md` all correctly use `<AppName>`-style placeholders in pragma; no hardcoded FinanceTracker paths/models leaked into the template
- Merge rule release/hotfix exemption (pragma PR #33): ported correctly into `pragma/scripts/setup.sh`'s CLAUDE.md-generation template — verified the exemption clause is present and matches FinanceTracker's `CLAUDE.md:47` wording
- Command file completeness: all files except `design.md` (flagged above) have Trigger, Process, and Done-when sections
