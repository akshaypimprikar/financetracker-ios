---
date: 2026-08-15
addressed: true
---

# Pipeline Review — 2026-08-15

## Critical
None found.

## High
- [x] pragma's `.claude/commands/parallel-review.md:5` and `pr-followup.md:9` hardcode the literal example project name "FinanceTracker" ("Confirmed on FinanceTracker 2026-07-30") instead of staying project-agnostic — the template is meant to read cleanly for any consumer project, and this leaks FinanceTracker-specific context into text that other adopters will read verbatim. — **Fix:** edit both lines directly in pragma (branch + PR — this text has no FinanceTracker-side equivalent to sync down from, so `/sync-workflow` won't catch it) to a generic phrasing, e.g. "Confirmed on a project using this pipeline, 2026-07-30." — Fixed in [pragma#41](https://github.com/akshaypimprikar/pragma/pull/41): both lines now end at "Check whether that applies to your setup" with the dated confirmation dropped entirely.

## Medium
None found.

## Low
- [x] `.claude/settings.json` has accumulated overly-broad and redundant permission entries. Overly broad: `Bash(python3 -c ' *)`, `Bash(cat > *)`, `Bash(git branch *)`, `Bash(gh api *)`. Redundant (already covered by a broader pattern already in the file): the eight `git -C .../pragma checkout|pull|merge|push|branch ...` entries are all subsumed by the existing `Bash(git -C /Users/akshaypimprikar/Desktop/Claude/pragma *)` pattern, and the two `GITFLOW_RELEASE_MERGE=1 git push ...` entries are subsumed by the existing `Bash(GITFLOW_RELEASE_MERGE=1 git *)` pattern. Session-specific one-off noise unrelated to the FinanceTracker pipeline is also present: `Bash(open /tmp/linkedin_banner.png)`, `Bash(python3 build_visual3.py)`, `Bash(sed -n '/## Post 9/,$p')`, and five near-duplicate `Bash(tee /tmp/*-taskN*.log)` entries. This is the exact gap `project_skills_to_introduce.md` already flagged as pending (the `fewer-permission-prompts` auto-prune extension). — **Fix:** build the auto-prune extension to `fewer-permission-prompts`, or manually move session-specific/redundant entries to the gitignored `.claude/settings.local.json`. — Partially fixed: removed the 8 redundant pragma `git -C` entries, the 2 redundant `GITFLOW_RELEASE_MERGE` entries, and all identified session-specific junk (9 stale `tee /tmp/*-taskN*.log` paths, `open /tmp/linkedin_banner.png`, `python3 build_visual3.py`, `sed -n '/## Post 9/,$p'`) — 20 entries removed total. The genuinely broad wildcards (`python3 -c '*`, `cat > *`, `git branch *`, `gh api *`) were deliberately left alone: no narrower replacement was identified, and removing them without one would just break legitimate future usage. The `fewer-permission-prompts` auto-prune extension itself remains unbuilt — still tracked in `project_skills_to_introduce.md`.
- [x] FinanceTracker's `.claude/commands/design.md` restructured the Bootstrap/Extend flow under one `## Process` heading with an explicit "two modes, selected by the trigger used" explainer and nested `### Bootstrap process` / `### Extend process` subheadings, but pragma's `design.md` still uses the older flat top-level `## Bootstrap process` / `## Extend process` headings with no mode explainer — a structural clarity improvement not yet back-ported. — **Fix:** run `/sync-workflow` to port the `## Process` restructuring into pragma's design.md. — Fixed in [pragma#41](https://github.com/akshaypimprikar/pragma/pull/41).
- [x] FinanceTracker's `gates.md` Gate 8 was replaced with an app-specific "CSV import concurrency shape" check (correctly, per `sync-workflow.md`'s own policy to keep app-specific gates out of the template), but this means FinanceTracker no longer runs any duplication/abstraction-bloat heuristic before opening a PR — pragma's Gate 8 (generic, advisory) still has one. — **Fix:** if the duplication heuristic is still wanted, add it back into FinanceTracker's gates.md as a new Gate 10 rather than leaving it dropped; otherwise no action needed, this is expected divergence. — Fixed: added back as Gate 10 (pragma's Gate 8 content, unchanged) rather than renumbering Gate 8/9 — avoids a cascading renumber across every "9 gates"/"Gate 9" reference in `gates.md`, `feature.md`, and `review.md`. Those references were bumped to "10 gates" instead.

## All clear
- Stale skill references: every skill named in FinanceTracker's `.claude/commands/*.md` (`simplify`, `ios-coverage`, `security-review`, `code-review:code-review`) appears in pipeline-review.md's valid-skills registry.
- CLAUDE.md token budget: 49 lines — within the ≤50 target.
- Memory staleness: every branch/PR reference found across `~/.claude/projects/-Users-akshaypimprikar-Desktop-Claude-FinanceTracker/memory/*.md` was checked against `git log`/`gh pr list` for both `financetracker-ios` and `pragma` and confirmed to still exist and match its described outcome — `feedback_gh_pr_create_head_flag.md` (pragma PR #32, closed as described, replaced by PR #34), `feedback_visuals_in_pr_descriptions.md` (FT PR #60, #61), `project_skills_to_introduce.md` (FT PR #67), `project_linkedin_series_state.md` (FT PR #68, #69, #70, #78). `MEMORY.md`'s index is current with all 48 memory files on disk, including the two most recently added (`project_pragma_phase4_platform_decision.md`, `feedback_crosscheck_established_facts.md`).
- Pipeline gate coverage: all 7 gates from the coverage table are present — `/gates` (`.claude/commands/gates.md`), Gate 1 build verification, CHANGELOG step in `/feature`, `/review`-before-`/test` ordering (confirmed in both `feature.md`'s Done-when and `CLAUDE.md`'s pipeline line), Gate 6 `ios-coverage`, Gate 7 `security-review`, and `/status` for session recovery.
- Command file completeness: all 15 files in FinanceTracker's `.claude/commands/` have Trigger, Process, and Done-when sections.
- Settings hygiene (hooks): the `PostToolUse` and `UserPromptSubmit` hooks in `.claude/settings.json` reference only inline shell commands and existing paths — no missing scripts. The `UserPromptSubmit` hook for the unaddressed-pipeline-review alert is present and correctly wired to `docs/pipeline-review/*.md`'s `addressed: false` frontmatter.
