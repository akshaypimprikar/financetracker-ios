---
date: 2026-08-16
addressed: true
---

# Pipeline Review — 2026-08-16

## Critical
- [x] `feature.md` requires invoking `Skill(test-driven-development)` before the RED step of every task, but `test-driven-development` is not in the current valid skill registry (checked against the full 30-skill list: plan, spec, design, review, feature, test, bugfix, release, gates, pipeline-review, sync-workflow, trim-context, simplify, security-review, code-review:code-review, ios-build-verify, ios-coverage, ios-swiftdata-test-fixture, update-config, keybindings-help, fewer-permission-prompts, schedule, loop, claude-api, init, claude-code-setup:claude-automation-recommender, run, verify, status) — `.claude/commands/feature.md:18` — **Fix:** either confirm the Superpowers `test-driven-development` skill is actually installed/invocable and add it to the registry this audit checks against, or replace the hard `Skill(test-driven-development)` invocation with the same git-history-based verification `/gates` Gate 11 now uses instead (note: `/gates` already dropped its own reliance on this skill name in favor of the RED-before-GREEN commit-order script — `feature.md` is the one file that didn't get the same update, and the identical stale reference also exists in pragma's template `feature.md:18`, so the fix should be ported to both).

## High
- [x] `.githooks/pre-push` exempts tag pushes from the "block push to a branch with an already-merged PR" rule but has no equivalent exemption for branch-deletion pushes — `.githooks/pre-push:16-19` (tag check) vs `.githooks/pre-push:29-37` (merged-PR block, which a delete push still hits since `remote_ref` for `git push origin --delete <branch>` is `refs/heads/<branch>`, not a tag ref) — **Fix:** add a delete-push exemption alongside the existing tag exemption, e.g. detect `local_sha` of all zeros (the standard signal for a delete push) and `continue` before the merged-PR check runs, so routine post-merge branch cleanup (`git push origin --delete <branch>`) stops requiring the `gh api -X DELETE` bypass workaround this session had to use.

## Medium
- [x] `reference_swiftdata_uitest_gotchas.md` is 72 days old and makes several specific, code-behavior-level claims (SwiftData `migrationPlan`+`isStoredInMemoryOnly` interaction, `UITestBase.tapWhenEnabled`, iOS 26 `Label`-in-`List` accessibility type, `UITestBase.timeout` value) with no re-confirmation since creation — it already carries a system-generated staleness warning at read time — `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude-FinanceTracker/memory/reference_swiftdata_uitest_gotchas.md` — **Fix:** this audit spot-checked all four claims against the current codebase (`FinanceTrackerApp.swift:17-30`, `UITestBase.swift:5,21`) and confirmed every one is still accurate — no content rewrite needed, just touch/re-date the file (or add a "confirmed valid as of 2026-08-16" note) so the 60-day staleness clock resets and future audits don't have to re-derive this.

## Low
- [x] `.claude/settings.json` grants blanket `WebFetch(*)` and `WebSearch(*)` permissions, which make the several scoped `WebFetch(domain:portal26.ai)`, `WebFetch(domain:www.gartner.com)`, `WebFetch(domain:martech.org)`, `WebFetch(domain:www.forbes.com)`, `WebFetch(domain:buffer.com)`, `WebFetch(domain:sproutsocial.com)` entries dead weight — the wildcard already permits everything those scoped entries would additionally allow — **Fix:** remove the domain-scoped entries (redundant clutter) or, if the intent was ever to tighten `WebFetch`/`WebSearch` to an allowlist, remove the `(*)` wildcards instead and keep only the scoped domains.

## All clear
- Template drift (pragma vs FinanceTracker): all 15 shared command files diffed cleanly — differences are all expected `<AppName>`-placeholder-vs-concrete-value swaps by design (per `/sync-workflow`'s own instructions). No FinanceTracker-specific literals (paths, `iPhone 17`, model/protocol names, `TransactionImportActor`) leaked into any pragma template file. Every FinanceTracker command file has a pragma counterpart. Gate numbering is internally consistent within each repo (FinanceTracker: 11 gates, all references agree; pragma: 10 gates, all references agree) — confirms pragma PR #42's sync landed cleanly with nothing left over.
- CLAUDE.md token budget: 49 lines — within the ≤50 target.
- Pipeline gate coverage: all seven required gates/steps verified present — `/gates` (`.claude/commands/gates.md`), Gate 1 build verification separate from Gate 2 test run, CHANGELOG `[Unreleased]` update step in `/feature`, `/review`-before-`/test` ordering documented in both `feature.md`'s Done-when and CLAUDE.md's pipeline line, `ios-coverage` skill wired into Gate 6, `security-review` skill wired into Gate 7 (conditional on CSVImport/Repository/SwiftData/UserNotification paths), and `/status` exists for session recovery.
- Command file completeness: all 15 files in `.claude/commands/` have Trigger, Process, and Done when sections.
- Settings hygiene — hooks: `PostToolUse` and `UserPromptSubmit` hooks are both self-contained inline scripts (no missing external file references); the `UserPromptSubmit` pipeline-review-alert hook is present and correctly wired to `docs/pipeline-review/`; scripts referenced elsewhere in permissions (`scripts/check_coverage.py`, `scripts/check_tdd_commit_order.py`) both exist; `core.hooksPath` is correctly set to `.githooks`; all three prior pipeline-review reports (`2026-05-23.md`, `2026-08-01.md`, `2026-08-15.md`) are marked `addressed: true`, so no backlog of ignored findings.
- Memory staleness — branch/PR/fixed-failure-mode checks: no memory file references a branch or PR that's absent from `git log`/`gh pr list`, and no memory describes a failure mode that command files have since silently fixed without the memory being updated (`feedback_gh_pr_create_head_flag.md`'s described fix is in fact codified in CLAUDE.md's Cross-repo rule, and `project_skills_to_introduce.md`'s claimed still-open gap — settings.json junk not yet auto-pruned — was independently confirmed still true by inspecting the current permissions list).
