---
date: 2026-09-09
addressed: true
---

# Pipeline Review — 2026-09-09

## Critical
- [x] `pipeline-review.md`'s own Memory staleness check (item 4) hardcodes the OLD per-project memory path, `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude-FinanceTracker/memory/`, instead of the current unified root-launch memory path. Per `project_root_launch_migration.md`, Claude Code sessions for this project now launch from the `~/Desktop/Claude` root, not per-project, so memory/session history live at `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude/memory/` — the old FinanceTracker-scoped directory was left in place as an untouched backup and has drifted stale (missing files added since the 2026-08-20 migration, e.g. `feedback_no_releases_for_forks.md`, `reference_mcp_registration_method.md`, `reference_obscura_setup.md`, `reference_skillspector_baselines.md`). A literal, unmodified run of `/pipeline-review` would read the stale directory and silently report zero memory findings regardless of actual staleness — same failure class as a stale skill reference. — `.claude/commands/pipeline-review.md:34` — **Fix:** change line 34 to read from `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude/memory/` instead. **Done 2026-09-09.**

## High
(none found)

## Medium
- [x] Template drift (pragma ahead, not yet pulled into FinanceTracker): pragma's `gates.md` carries an entire `## A known limitation: no native guard against self-modifying guardrail files` section — documenting that nothing in the pipeline uses a native `PreToolUse` hook to block a `Write`/`Edit` call against `gates.md`, `CLAUDE.md`, or `.claude/context/invariants.md` during an autonomous `/loop` run — that was never pulled into FinanceTracker's `gates.md`. This isn't just a doc gap: FinanceTracker's own `.claude/settings.json` `PreToolUse` hooks only match `Bash` commands (the `gh pr create` junk-check), confirming the actual guardrail-file protection this section calls for is also missing here, not just undocumented. — **Fix:** port the "known limitation" section into FinanceTracker's `gates.md`, and separately consider whether to implement the `PreToolUse` hook it describes. **Done 2026-09-09** — section ported; implementing the hook itself is a separate architecture decision, not done here.
- [x] Template drift (pragma ahead, not yet pulled into FinanceTracker): pragma's `review.md` has two whole sections FinanceTracker's copy lacks — `## A known tradeoff: context continuity, not context isolation` (documents that `/review` typically runs in the same session as `/feature`/`/gates`, trading reviewer isolation for external GitHub-auditability, and how to get real isolation if wanted) and `## Tip — automate the review-fix loop` (a ready-to-use `/loop 5m "Check PR <N> for new review comments or failing CI..."` pattern). — **Fix:** port both sections into FinanceTracker's `review.md`. **Done 2026-09-09.**
- [x] Template drift (pragma ahead, not yet pulled into FinanceTracker): pragma's `trim-context.md` has an entire `### 5. Runtime read/tool-output waste (diagnostic only, no proxy required)` section (an optional `headroom audit-reads` diagnostic that sizes token waste by category) that FinanceTracker's `trim-context.md` lacks entirely — FinanceTracker's `## Output` section still reads "For each section" rather than pragma's "For each section, including section 5," consistent with the section's absence. — **Fix:** port section 5 (and the corresponding Output-section wording) into FinanceTracker's `trim-context.md`. **Done 2026-09-09.**
- [x] Memory staleness: `feedback_gh_pr_create_head_flag.md` presents the pragma#32 `gh pr create --head` bug as a lesson to actively apply, but the fix has since become the source of truth in the command files themselves — CLAUDE.md's "Cross-repo rule" already codifies "always pass `--head` explicitly... verify with `gh pr view <N>`," and FinanceTracker's own `sync-workflow.md` already has the `--head` flag plus a "Verify before trusting the PR" step (see the sync-workflow.md drift finding below — that fix just hasn't reached pragma's copy yet). The memory itself is now redundant with the codified fix. — `/Users/akshaypimprikar/.claude/projects/-Users-akshaypimprikar-Desktop-Claude/memory/feedback_gh_pr_create_head_flag.md` — **Fix:** mark this memory as historical/resolved (the rule lives in CLAUDE.md now) rather than standalone actionable guidance. **Done 2026-09-09.**

## Low
- [x] Template drift (FinanceTracker ahead, not yet back-ported to pragma): FinanceTracker's `sync-workflow.md` has a whole `### 5. Self-review the diff before committing` step (checks for FinanceTracker-literal leaks in the staged template diff, confirms `<placeholder>` convention is held, and verifies gate numbering/counts stay internally consistent) plus a `--head <branch>` flag on `gh pr create` and a "Verify before trusting the PR" step using `gh pr view` — none of this exists in pragma's `sync-workflow.md`, which still reads as the pre-2026-08 version (dated 2026-06-10, vs. FinanceTracker's 2026-08-16). — **Fix:** run `/sync-workflow` to port these improvements into pragma's copy. **Done 2026-09-09.**
- [x] Template drift (FinanceTracker ahead, not yet back-ported to pragma): FinanceTracker's `release.md` added a `### 5. Verify the release branch only touches release files` safety check — confirms a `release/*` branch's diff only touches `FinanceTracker.xcodeproj/project.pbxproj`, `CHANGELOG.md`, or `.claude/context/feature-log.md` before opening the PR, given CLAUDE.md's Merge rule exempts `release/*` PRs from `/review` and `code-review:code-review` on the assumption they carry no new logic. This is a generalizable safety check (any pragma-scaffolded project with the same release-branch review exemption benefits from it) but pragma's `release.md` is still the pre-2026-06-09 version with no such step. — **Fix:** back-port this step into pragma's `release.md` template, using `<AppName>`-style placeholders for the file list. **Done 2026-09-09.**
- [x] **Correction 2026-09-09:** an earlier pass of this fix added `-enableCodeCoverage YES` back to `release.yml` based on Akshay's answer to a question that omitted a fact discoverable in git history: commit `82a246c` (2 days earlier) already investigated this exact gap and explicitly decided NOT to port it, after confirming locally that this repo's scheme already collects non-zero coverage (89.8% on `BalanceService.swift`) without the flag. Reverted before merge — `code-review:code-review` caught the conflict by cross-referencing git history, not just the diff. No unresolved gap remains; pragma's scaffold still carries the flag as the generic template default, which is fine since a template can't know a consuming project's scheme already collects coverage by default.
- [x] Settings hygiene: `.claude/settings.json`'s permissions allow-list grants several `Bash(git <verb> *)` patterns with no repo scoping at all — `git push *`, `git pull *`, `git tag *`, `git branch *`, `git merge *`, `git fetch *`, `git cherry-pick *`, `git config core.hooksPath *` — meaning these apply to *any* git repo on the filesystem, not just FinanceTracker/pragma, even though the tighter `Bash(git -C /Users/akshaypimprikar/Desktop/Claude/FinanceTracker *)` and `Bash(git -C .../pragma *)` patterns already exist and would cover the same legitimate uses more precisely. — `.claude/settings.json` (permissions.allow) — **Fix:** replace the unscoped git-verb wildcards with `-C`-scoped equivalents, or confirm they're intentionally broader (e.g. for occasional other-repo work) and leave a comment/note explaining why. **Done 2026-09-09** — Akshay confirmed intentional (covers occasional work in other tracked repos); JSON has no native comment syntax, so the rationale is documented in memory (`reference_unscoped_git_permissions_intentional.md`) instead of inline.

## All clear
- Stale skill references: every skill/command name referenced across all 16 `.claude/commands/*.md` files (`ios-coverage`, `security-review`, `simplify`, `code-review:code-review`, and every `/slash-command`) resolves to a name in the current valid registry.
- Template drift — app-specific leaks in pragma: pragma's `.claude/commands/*.md` files are consistently genericized (`<AppName>`, `<Model>`, `<RepositoryProtocol>`-style placeholders); the only literal mentions of "FinanceTracker" in pragma's commands are inside `pragma-review.md`'s and `sync-workflow.md`'s own self-review grep patterns (checking *for* such leaks) and pragma's own `--repo akshaypimprikar/pragma` self-references — not actual leaked content.
- Template drift — missing template equivalents: every command file present in FinanceTracker's `.claude/commands/` has a counterpart in pragma's `.claude/commands/` (pragma additionally carries `init.md` and `pragma-review.md`, pragma-only tooling with no FinanceTracker equivalent needed).
- Gate numbering cross-check confirmed: pragma's template numbers Gate 8 = Abstraction bloat, Gate 9 = RED-before-GREEN, Gate 10 = Architecture (template); FinanceTracker's instantiated numbering is Gate 8 = CSV import concurrency shape (app-specific, correctly left out of the template per pragma's own `sync-workflow.md` guidance), Gate 9 = Architecture, Gate 10 = Abstraction bloat, Gate 11 = RED-before-GREEN. Every cross-reference to these gate numbers in both repos' `review.md`, `parallel-review.md`, `plan.md`, and `feature.md` correctly uses its own repo's numbering — no stale cross-reference found in either direction.
- CLAUDE.md token budget: 49 lines — within the ≤50 target.
- Pipeline gate coverage: all seven required gates/steps present and correctly wired — `/gates` exists (`.claude/commands/gates.md`), Gate 1 is build verification, `/feature` has a CHANGELOG step, `/review`-before-`/test` ordering is documented in both `feature.md`'s Done-when and `CLAUDE.md`'s pipeline description, `ios-coverage` is wired into Gate 6, `security-review` is wired into Gate 7, and `/status` exists for session recovery.
- Command file completeness: all 16 files in `.claude/commands/` have Trigger, Process, and Done when sections.
- Settings hygiene — hooks: all four hooks (`PostToolUse`, `PreToolUse`, `SessionEnd`, `UserPromptSubmit`) are self-contained inline shell referencing only files confirmed to exist (the settings.json file itself, `docs/pipeline-review/`); none reference a missing external script.
- Settings hygiene — pipeline-review alert hook: present and functioning as designed.
