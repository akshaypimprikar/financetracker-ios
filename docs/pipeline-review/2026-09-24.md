---
date: 2026-09-24
addressed: true
---

# Pipeline Review — 2026-09-24

Post-release run after FinanceTracker v1.4.0 (pragma v2.0.0 released the same day). Both repos moved commands to `.claude/skills/` and moved to AGENTS.md recently (FinanceTracker `66e98a8`, 2026-09-23). Already tracked, noted here but not re-checked: the README CI badge shows a stale May failure. The badge has no event or branch parameter, and `pr-checks.yml` only runs on `pull_request`.

## Critical
- [x] Registry gap, not a broken reference. `deterministic-pr-gates` is named as a skill in `sync-workflow`, but it is not on this skill's valid-skills list. The skill does exist in pragma (`pragma/.claude/skills/deterministic-pr-gates/SKILL.md`), and pragma's copy of the list already includes it (`pragma/.claude/skills/pipeline-review/SKILL.md:24`). It is listed under Critical only because §1 sets that severity. — `.claude/skills/sync-workflow/SKILL.md:82` (also `:83`, `:90`; list at `.claude/skills/pipeline-review/SKILL.md:24`) — **Fix:** Add `deterministic-pr-gates` to the valid-skills list at `pipeline-review/SKILL.md:24`. Leave the sync-workflow text as it is. **Fixed 2026-09-24** — added to the list in this PR.

## High
(none found)

## Medium
- [x] Template drift (fixed in pragma, not yet pulled into FinanceTracker): `release/SKILL.md:7-9` and `gates/SKILL.md:7-9` each have a stray second `---`/`model: claude-haiku-4-5-20251001`/`---` block after the real frontmatter closes at line 5. Only the first frontmatter block is parsed, so the Haiku routing is silently ignored and the block is sent to the model as body text. pragma moved `model:` into the single frontmatter block (`pragma/.claude/skills/release/SKILL.md:4`, `pragma/.claude/skills/gates/SKILL.md:4`). — **Fix:** Move the `model:` line into the first frontmatter block in both files and delete lines 6-9. **Fixed 2026-09-24** — `model:` moved into the first frontmatter block of both skills.
- [x] Template drift (fixed in pragma, not yet pulled into FinanceTracker): Gate 2's pass/fail counters only match the XCTest format `^Test [Cc]ase '…' passed|failed` (`gates/SKILL.md:71`). pragma's Gate 2 (`pragma/.claude/skills/gates/SKILL.md:73`) also matches Swift Testing's `✔/✓ Test … passed` and `✘/✗ … failed` lines. FinanceTracker's own `/test` already uses the wider pattern (`test/SKILL.md:71`), so `/gates` and `/test` count tests differently. Not verified: which line format Xcode 27's `xcodebuild` prints for this repo's Swift Testing suites, because this review did not run a test. — **Fix:** Use the same wider regex in Gate 2 (and in the ImportHash golden-test counter at `gates/SKILL.md:218`) that `test/SKILL.md:71` uses, then check the count once against a real green run. **Fixed 2026-09-24** — both counters now use the wider regex. Checked against the real 2026-09-24 run on Xcode 27: it prints `Test case '…' passed` for this repo's Swift Testing suites (191 such lines, 0 `✔` lines), so the count was already right; this is consistency with `/test`, not a missed-tests bug. Golden counter: 10.
- [x] Template drift (in pragma, not yet pulled into FinanceTracker): pragma's `/feature` has a **doubt-driven review** step before the GREEN commit (`pragma/.claude/skills/feature/SKILL.md:27`). A fresh-context subagent gets only the diff and the acceptance criteria, and runs for new protocols and `@Model` mutations. Up to 2 rounds, and real findings are logged to `incidents.md`. FinanceTracker's `feature/SKILL.md` has no such step (grep for "doubt" finds nothing in any FinanceTracker skill). — **Fix:** Port the step into `feature/SKILL.md`'s per-task rules (after line ~26) and use FinanceTracker's gate numbers (Gate 9 architecture, Gate 10 abstraction). **Fixed 2026-09-24** — ported with FinanceTracker's gate numbers (Gate 10 protocols, Gate 9 `@Model`).
- [x] Template drift (fixed in pragma, not yet pulled into FinanceTracker): `/feature`'s `/loop` stop condition names only 7 criteria even though it says "all 13 gates pass". Coverage, security, CSV concurrency shape, architecture compliance and gate integrity are missing (`feature/SKILL.md:65`). Lines 57, 61 and 65 also say "all 13 gates pass", but `gates/SKILL.md:436-438` marks Gates 10 and 12 as advisory ("11 blocking gates pass"). pragma's version lists every gate (`pragma/.claude/skills/feature/SKILL.md:66`). — **Fix:** Use the full stop-condition list from `gates/SKILL.md:276` and say "all blocking gates", as `gates/SKILL.md:282` already does. **Fixed 2026-09-24** — stop condition lists every blocking gate; Done when and both loops say "all blocking gates".
- [x] Template drift (fixed in pragma, not yet pulled into FinanceTracker): FinanceTracker's hotfix path says only "also open a second PR to `develop`" (`bugfix/SKILL.md:79`). pragma's version (`pragma/.claude/skills/bugfix/SKILL.md:80`) says both hotfix PRs must pass `/review`, `/test` and `code-review:code-review`. It also explains why: a hotfix's commits were never reviewed on `develop`, so the back-merge cannot be a raw merge the way a release back-merge can. — **Fix:** Port pragma's two-line Regular/Hotfix split into `bugfix/SKILL.md:79`. **Fixed 2026-09-24** — Regular/Hotfix split ported.
- [x] Template drift (in pragma, not yet pulled into FinanceTracker): pragma's `/review` Design checklist has an advisory layout-adaptivity item. It covers hardcoded sizes, safe areas, and `#available` gating for APIs newer than the deployment target (`pragma/.claude/skills/review/SKILL.md:92`). FinanceTracker's Design checklist (`review/SKILL.md:88-93`) does not have it. — **Fix:** Add the item to the Design checklist in `review/SKILL.md`. **Fixed 2026-09-24** — advisory layout item added (plus pragma's exit-2 note on the TDD script).
- [x] Template drift (in pragma, not yet pulled into FinanceTracker): pragma's Gate 1 has an advisory note pointing to `docs/xcode-27-sdk-migration.md` (the file exists in pragma) for SwiftUI compile errors caused by Xcode 27 SDK source-compatibility breaks (`pragma/.claude/skills/gates/SKILL.md:61-64`). FinanceTracker's Gate 1 (`gates/SKILL.md:47-63`) does not have it. Xcode 27.0 (27A266a) is the installed Xcode, so this applies now. — **Fix:** Port the advisory paragraph to the end of FinanceTracker's Gate 1. **Fixed 2026-09-24** — advisory paragraph added, linking pragma's doc.
- [x] Template drift (fixed in pragma, not yet pulled into FinanceTracker): FinanceTracker skills use `AGENTS.md/CLAUDE.md` 14 times as if it were one file path (for example `trim-context/SKILL.md:18-19`, `pipeline-review/SKILL.md:35-36`, `sync-workflow/SKILL.md:29`). This looks like a leftover from the migration's find-and-replace. pragma replaced it with "`AGENTS.md` (or `CLAUDE.md` on the pre-AGENTS.md convention)" in the same places (`pragma/.claude/skills/trim-context/SKILL.md:18-19`, `pragma/.claude/skills/pipeline-review/SKILL.md:36`, `pragma/.claude/skills/sync-workflow/SKILL.md:29`). In FinanceTracker, `CLAUDE.md` is a one-line `@AGENTS.md` import, so the real target is `AGENTS.md`. — **Fix:** Replace the pseudo-path with `AGENTS.md`, at least where it is an instruction to read or edit the file (trim-context §1 edits it in place). **Fixed 2026-09-24** — replaced with `AGENTS.md` everywhere (39 occurrences, not 14).
- [x] Memory staleness: `feedback_scrapling_ask_before_use.md` says `~/.claude/settings.json` "still has `mcp__ScraplingServer__*` under `permissions.ask`". Today the global `permissions.ask` has no Scrapling entry, `permissions.allow` has `mcp__scrapling__*`, and the MCP server itself is no longer registered at user scope (`reference_mcp_ondemand_scoping.md`). `reference_scrapling_setup.md` already has the corrected facts. — **Fix:** Delete the stale "Note:" sentence and point to `reference_scrapling_setup`. **Fixed 2026-09-24** — stale note replaced with the verified current setup.
- [x] Memory staleness: `project_xcode27_agent_skills_pending.md` (and its MEMORY.md index line) says the work is "blocked on Xcode 27 actually being installed, which hasn't happened yet". `xcodebuild -version` now reports Xcode 27.0 (27A266a), and `gates/SKILL.md` refers to a 2026-09-20 Xcode 27 check. — **Fix:** Update the status, or turn it into the Backlog card the memory says to create once Xcode 27 is installed. Akshay decides. **Resolved 2026-09-24** — Xcode 27.0 is installed, but `xcrun agent skills export` does not exist in 27A266a (`xcrun agent` is `mcpbridge`, only `run-agent`). Memory updated to say it is still blocked on the command; Akshay chose no board card yet.
- [x] Memory staleness: `project_root_launch_migration.md`. Line "NOT yet empirically verified: whether a project's own `.claude/settings.json`… still fires" is contradicted later in the same file ("Confirmed consequence… 2026-08-30"). The "Commands" and "Hooks" bullets describe `.claude/commands/*.md` and a PostToolUse hook watching `.claude/commands/`. Both repos now use `.claude/skills/`, and the hook greps `\.claude/skills/` (`FinanceTracker/.claude/settings.json` PostToolUse). — **Fix:** Delete the superseded "not yet verified" bullet, and change the commands paths to skills. **Fixed 2026-09-24** — superseded bullet struck; dated note on the skills migration added (history kept).
- [x] Memory staleness: `feedback_auto_sync_workflow.md` triggers on "any change to `.claude/commands/` files, `CLAUDE.md`". After the migration those are `.claude/skills/*/SKILL.md` and `AGENTS.md`, so the trigger as written no longer matches the files that change. (Undated, so its age is unknown.) — **Fix:** Update the trigger paths. **Fixed 2026-09-24** — trigger now `.claude/skills/*/SKILL.md` and `AGENTS.md`/`CLAUDE.md`.
- [x] Memory staleness: `feedback_code_review_scope.md` scopes the rule to PRs touching "`.claude/commands/`, `scripts/`" and `.claude/commands/*.md`. Now those paths are `.claude/skills/*/SKILL.md`. — **Fix:** Update the paths. **Fixed 2026-09-24** — scope now `.claude/skills/`, `.claude/commands/`, `.claude/hooks/`, `scripts/`, `scaffold/`.
- [x] Memory staleness: `feedback_token_optimization.md` says "Keep CLAUDE.md under 50 lines" and "anything already in `.claude/commands/`". FinanceTracker's budgeted file is now `AGENTS.md` (49 lines; `CLAUDE.md` is a 1-line import), and commands are now skills. The MEMORY.md index line has the same wording. — **Fix:** Re-point it to AGENTS.md and skills. **Fixed 2026-09-24** — memory and index line point at AGENTS.md (or CLAUDE.md if not migrated) and `.claude/skills/`.
- [x] Memory staleness: `project_linkedin_series_state.md` has a frontmatter description ("as of 2026-08-10 — 23 published… scheduled Aug 11") and a heading ("Published posts (23 total; last published Aug 6 2026)") that disagree with its own body ("2026-09-23 update — 26 published"). The MEMORY.md index line says "25 posts live as of 2026-09-22". — **Fix:** Update the description, the heading and the index line to one current count. **Fixed 2026-09-24** — description, heading and index line all say 26 published as of 2026-09-23.
- [x] Memory staleness (>60 days, specific factual claim): `reference_swiftdata_inheritance_cloudkit_risk.md` states the status of FB21576369, FB21576779 and FB21837287 as "unconfirmed as of this note". The note has no `modified` date, and its only dated anchor is 2026-07-15 (71 days ago). — **Fix:** Re-check the Apple Forums thread #812212 status, or mark the note as unverified and undated. **Deferred 2026-09-24** — marked as last checked ~2026-07-15 and unverified since; not re-checked because the risk is dormant (only relevant if `Account` inheritance is proposed, and the memory already says to re-check then).
- [x] Memory staleness: `project_skills_to_introduce.md` describes a finished task ("all done… No remaining open scope", PR #99 merged), and it is not listed in MEMORY.md, so it never loads. — **Fix:** Delete it, or fold the one useful fact (PR #99 added the prune nudge hooks) into `feedback_pr_workflow_discipline` or the settings memory. **Fixed 2026-09-24** — PR #99 fact folded into `feedback_pr_workflow_discipline`; file deleted.
- [x] Memory staleness, grouped as one entry because each is a small stale reference: `feedback_platform_agnostic_new_mechanisms.md` refers to `gates.md` (now `gates/SKILL.md`). `project_root_claude_md.md` says "don't duplicate… `.claude/commands/`". `feedback_check_kanban_before_task.md` says "pragma command files". `feedback_gh_pr_create_head_flag.md` cites "CLAUDE.md's Cross-repo rule" and "pragma's `sync-workflow.md`" (now AGENTS.md and `sync-workflow/SKILL.md:118`). `feedback_proactive_release_reminders.md` links the non-existent `[[project_pragma_first_release]]`. `feedback_crosscheck_established_facts.md` links the non-existent `[[feedback_no_fabricated_claims]]`. — **Fix:** Correct each path or link in one pass. **Fixed 2026-09-24** — each path/link corrected (`feedback_no_fabricated_claims` → `feedback_verify_and_source_claims`; the `project_pragma_first_release` reference removed).

## Low
- [x] Template drift (FinanceTracker ahead, not back-ported): pragma's `pipeline-review/SKILL.md` still says "command file" at `:26`, `:48` and `:60-61` ("Command file completeness"). FinanceTracker updated these to "skill file" after the migration. — **Fix:** Back-port the wording in the next `/sync-workflow`. **Fixed 2026-09-24** — in the pragma PR.
- [x] Template drift (FinanceTracker ahead, not back-ported): pragma's "Write candidate invariants" says to append to `invariants.md` and commit on every branch (`pragma/.claude/skills/gates/SKILL.md:314`). pragma's own guard hook blocks that edit on `feature/*`. FinanceTracker's version sends feature-branch candidates to the PR body instead (`gates/SKILL.md:387-389`). — **Fix:** Back-port FinanceTracker's two-bullet branch split. **Fixed 2026-09-24** — in the pragma PR.
- [x] Template drift (FinanceTracker ahead, not back-ported): FinanceTracker's `/release` pre-flight has a runnable force-unwrap check (`release/SKILL.md:36-39`). pragma has only the checklist line with no command (`pragma/.claude/skills/release/SKILL.md:30`). — **Fix:** Back-port it with a `<AppName>/*.swift` path. **Fixed 2026-09-24** — in the pragma PR (`<AppName>/*.swift`).
- [x] Stale post-migration file references (both repos). `/review` says "exactly as written in `gates.md`" (`review/SKILL.md:67`, `:133`; pragma `review/SKILL.md:67`, `:132`), but that file is now `gates/SKILL.md`. It also cites "`gates.yml` does the same" (`review/SKILL.md:49`), but no `gates.yml` exists. The job is `gates` inside `.github/workflows/pr-checks.yml:92`, and pragma already dropped this clause. — **Fix:** Change these to `gates/SKILL.md` and "the `gates` job in `pr-checks.yml`". **Fixed 2026-09-24** — `gates/SKILL.md` and "the `gates` job in `pr-checks.yml`" in both repos.
- [x] Settings hygiene, over-broad allow rules (git verbs left out on purpose, see `reference_unscoped_git_permissions_intentional`): `Bash(rm -rf /tmp/debug_ui.xcresult *)` and `Bash(rm -rf /tmp/FinanceTracker.xcresult *)` (`.claude/settings.json:130-131`). The trailing ` *` also allows any extra path after `rm -rf`. `Bash(cat > *)` (`:121`) allows writing any file through shell redirection. `Bash(python3 -c ' *)` (`:66`) and `Bash(python3 -)` (`:93`) allow running any code. `Bash(gh repo *)` (`:89`), `Bash(gh release *)` (`:22`) and `Bash(gh issue *)` (`:127`) include the `delete` subcommands. `Bash(pip3 install *)`, `Bash(uv pip *)`, `Bash(brew tap *)` and `Bash(brew install *)` (`:50-51`, `:85-86`) allow installing any package. — **Fix:** Narrow the `rm -rf` entries to exact paths (no trailing `*`), and change the others to specific subcommands (for example `gh release view *`, `gh release create *`), or move them to `ask`. **Deferred 2026-09-24** — Akshay chose to keep these rules; recorded in `reference_unscoped_git_permissions_intentional` so reviews stop re-raising them.
- [x] Settings hygiene, one-off session entries that the junk-detector regex does not catch (it only matches tmp/PID/window-id shapes): `.claude/settings.json:10` (superpowers `find … | head -20`), `:67-68` (`~/.claude/commands` checks), `:97-98` (Cursor.app reads), `:100` (a system-fonts `ls`/`find`), `:137-138` (one-off `python3 -c` and `grep "iPhone 16"`). — **Fix:** Run `fewer-permission-prompts` in prune mode to move these to `settings.local.json`. **Fixed 2026-09-24** — the 8 entries moved to `settings.local.json`.
- [x] Settings hygiene, outside the strict §7 scope: the global `~/.claude/settings.json` has its own UserPromptSubmit pipeline-review alert. It uses the older `grep -l '^addressed: false'` check, which misses `addressed: "false"` and trailing comments and matches outside the frontmatter. FinanceTracker's hook and `/status` were hardened against those cases (pragma `660d8ea`). In a session launched from FinanceTracker, both the global and the project hook fire, so the alert can show twice. The same double firing applies to the PreToolUse and SessionEnd junk checks. — **Fix:** Update the global hook to use the frontmatter-scoped awk check, and decide which copy should exist. **Fixed 2026-09-24** — the global hook uses the frontmatter-only check and, from a non-repo cwd (root launch), scans `*/docs/pipeline-review/`; the FinanceTracker-only root copy was removed (it was the only alert that worked from root). Sessions launched inside FinanceTracker still get it twice (global + in-repo copy), accepted.
- [x] Memory index: `feedback_gh_pr_create_head_flag.md` and `project_skills_to_introduce.md` exist in the memory folder but are not listed in `MEMORY.md`, so they never load. — **Fix:** Index them or delete them. See the related Medium items. **Fixed 2026-09-24** — `feedback_gh_pr_create_head_flag` indexed; `project_skills_to_introduce` deleted.

## All clear
- Stale skill references: apart from the registry gap above, every `/slash-command` and named skill in the 16 FinanceTracker `SKILL.md` files is on the valid list. `/babysit` (`review/SKILL.md:161`) is explicitly described as not a command. `headroom` is a CLI tool, not a skill. The `/dev` matches are `/dev/null` redirects.
- Template drift, app-specific leaks in pragma: no hardcoded FinanceTracker paths, models, protocols, simulator names or `/Users/akshaypimprikar` paths in `pragma/.claude/skills/`. The only literal "FinanceTracker" is the worked example inside `sync-workflow/SKILL.md:72`'s leak-check instructions, which is intended.
- Template drift, missing template equivalents: all 16 FinanceTracker skills have a pragma counterpart. pragma also has `deterministic-pr-gates`, which is pragma-only.
- Gate numbering: FinanceTracker (8 = CSV concurrency, 9 = architecture, 10 = abstraction, 11 = TDD order, 12 = visual, 13 = integrity) and pragma (8 = abstraction, 9 = TDD, 10 = architecture, 11 = integrity) are each consistent within their own repo. Per the 2026-09-09 review, the difference is intended.
- AGENTS.md/CLAUDE.md token budget: `AGENTS.md` is 49 lines and `CLAUDE.md` is a 1-line `@AGENTS.md` import, within the ≤50 target.
- Pipeline gate coverage: all 7 are present. `/gates` exists (`gates/SKILL.md`). Gate 1 is the build (`:47`). The CHANGELOG entry is part of `/feature`'s GREEN commit (`feature/SKILL.md:30`). `/review` → `/test` → `code-review:code-review` runs in sequence (`feature/SKILL.md:57`, AGENTS.md "Standard pipeline"). `ios-coverage` is in Gate 6 (`gates/SKILL.md:109`). `security-review` is in Gate 7 and matches CSVImport, Repository, SwiftData and UserNotification files (`gates/SKILL.md:114-116`). `/status` exists.
- Skill file completeness: all 16 skills have Trigger, Process and Done when sections.
- Settings hygiene, hooks: every hook is inline shell, except `.claude/hooks/guard_protected_paths.py`, which exists. `jq` (`/usr/bin/jq`) and `python3` are on PATH.
- Settings hygiene, pipeline-review alert: the `UserPromptSubmit` hook is present in `.claude/settings.json` and also in the root `~/Desktop/Claude/.claude/settings.json`, for sessions launched from the root.
- Memory references to PRs and commits: all resolve. FinanceTracker #48, #49, #60, #61, #92, #93, #94, #99 and #100 are merged. pragma #32 is closed, #46, #47 and #48 are merged, #72 is open. Commit `f17bc6a` exists.

## Notes on coverage
- Memory: all 85 files in the memory folder were checked. 78 were read in full. The 7 longest LinkedIn and job-search files (`project_job_search_skill_suite`, `feedback_linkedin_voice_rules`, `feedback_linkedin_post_style_engagement`, `project_linkedin_series_state`, `feedback_verify_and_source_claims`, `reference_resume`, `feedback_linkedin_revision_pushback`) were checked by their frontmatter plus a grep for PR, branch, path and "as of" claims, not read line by line.
- Memory age check (>60 days): only files with a `modified` date or a dated anchor could be aged. About 20 files have neither (for example `user_profile`, `project_job_search_strategy`, `feedback_production_standard`), so their age could not be checked. None of the dated files is older than 60 days except the one flagged above.
- The Gate 2 regex finding was confirmed from the files only. No `xcodebuild test` was run to see which result-line format Xcode 27 actually prints.
- The PushNotification step was skipped on purpose (results are passed on by the caller).

## Resolution (2026-09-24)
All 27 items are fixed or deferred with a reason. Fixes landed in the FinanceTracker and pragma `chore/pipeline-review-2026-09-24` PRs, the memory store, and the global/root Claude settings.
