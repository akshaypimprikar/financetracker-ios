---
date: 2026-05-23
addressed: true
---

# Pipeline Review — 2026-05-23

## Critical
- [x] Both `pipeline-review.md` files omit `run`, `verify`, and `status` from their valid skill registries — `.claude/commands/pipeline-review.md:18` and `/Users/akshaypimprikar/Desktop/ios-agent-workflow/.claude/commands/pipeline-review.md:18` — **Fix:** Append `, `run`, `verify`, `status`` to the valid skills list in both files so future audits correctly flag or clear references to these skills.

## High
- [x] `ios-agent-workflow/test.md` contains hardcoded `FinanceTrackerTests/<Layer>/` and `FinanceTrackerUITests/` instead of generic placeholders — `/Users/akshaypimprikar/Desktop/ios-agent-workflow/.claude/commands/test.md:28-29` — **Fix:** Replace with `<AppName>Tests/<Layer>/` and `<AppName>UITests/` to keep the template app-agnostic.

## Medium
- [x] `feedback_always_create_pr.md` cites specific historical incidents (PRs #4, #6, README direct push on 2026-05-11) that are fully resolved and no longer actionable — memory file — **Fix:** Trim the "Why" narrative to remove resolved incident references; retain the behavioral rules only.
- [x] `feedback_check_pr_on_correction.md` describes a historical incident where `security-review` was replaced by `code-review:code-review` and ios-agent-workflow PR #1's body was stale — memory file — **Fix:** Trim to the actionable rule (always update open PR titles and bodies after corrections); remove the incident backstory.
- [x] `gates.md` Done-when section says "All 5 gates pass" but the file defines 7 gates — `.claude/commands/gates.md:101` — **Fix:** Update Done-when to read "All 7 gates pass, PR is open, and the PR URL is returned to the user."

## Low
- [x] `ios-agent-workflow/plan.md` is missing the test framework enforcement rule (`import Testing` with `@Suite`/`@Test`/`#expect()` — NOT XCTest) that exists in FinanceTracker's `plan.md:42` — `/Users/akshaypimprikar/Desktop/ios-agent-workflow/.claude/commands/plan.md` — **Fix:** Add `- Test framework: \`import Testing\` with \`@Suite\`/\`@Test\`/\`#expect()\` — NOT XCTest for unit tests` to the Architecture Rules section; run `/sync-workflow` after.
- [x] `Bash(git config *)` permission in `settings.json` is overly broad — allows any git config mutation — `.claude/settings.json:32` — **Fix:** Narrow to `Bash(git config core.hooksPath *)` or the specific config patterns actually needed.
- [x] `Bash(brew install *)` and `Bash(brew list *)` in `settings.json` allow arbitrary package installation — `.claude/settings.json:12-13` — **Fix:** Replace with specific brew patterns actually used (e.g. `Bash(brew list gh)`) or remove if brew commands are not part of the agent workflow.

## All clear
- Stale skill references: no command file references a skill outside the valid registry (all skill invocations — `simplify`, `ios-coverage`, `security-review`, `code-review:code-review` — are on the valid list)
- CLAUDE.md token budget: 50 lines — exactly at target, no trim needed
- Pre-PR gate (`/gates`): present and complete with 7 ordered gates
- Build verification: Gate 1 of `gates.md` — present and correct
- CHANGELOG incremental update: documented in `feature.md` per-task rules and `bugfix.md`
- `/review` before `/test` ordering: correctly enforced in `feature.md` Done-when and `CLAUDE.md` pipeline description
- Coverage check: `ios-coverage` skill wired into Gate 6 of `gates.md` and `review.md`
- Security check: `security-review` skill wired into Gate 7 of `gates.md` (conditional on CSVImport/Repository/SwiftData/UserNotification file changes)
- Session recovery (`/status`): `status.md` present and complete
- Command file completeness: all 13 command files in `.claude/commands/` have Trigger, Process, and Done-when sections
- Settings hooks: `PostToolUse` sync-workflow reminder hook and `UserPromptSubmit` pipeline-review alert hook both present; `.githooks/pre-push` file exists at referenced path
- Memory: `feedback_gh_pr_base_develop.md`, `feedback_ios_agent_workflow_pr.md`, `feedback_never_substitute_skill_names.md`, `feedback_production_standard.md`, `feedback_token_optimization.md`, `project_skills_to_introduce.md` — all current and actionable
