# Charts Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Add a running balance line chart to `AccountDetailView` and a month-over-month spending bar chart to `BudgetDetailView` using Swift Charts.
**Architecture:** Two new pure methods on existing services produce typed value objects; ViewModels convert `Decimal` → `Double` and expose chart-ready tuples; Views embed Swift Charts inside new `Section`s in the existing `List`.
**Tech Stack:** Swift Charts (system framework, iOS 26+), SwiftUI, SwiftData (repositories only), Swift Testing.
**All commands run from:** `/Users/akshaypimprikar/Desktop/FinanceTracker/` (git root, contains `FinanceTracker.xcodeproj`)

---

## Task 1 — Add `BalanceDataPoint` and `runningBalance` to `BalanceService`

**File:** `FinanceTracker/Services/BalanceService.swift`

Add a new value type and method. No SwiftData imports — the method is a pure function.

Replace the entire file with:

```swift
import Foundation

struct BalanceDataPoint: Identifiable {
    let id: UUID
    let date: Date
    let balance: Decimal

    init(date: Date, balance: Decimal) {
        self.id = UUID()
        self.date = date
        self.balance = balance
    }
}

struct BalanceService {
    func balance(for account: Account, transactions: [Transaction]) -> Decimal {
        var result = account.openingBalance
        for tx in transactions {
            switch tx.type {
            case .credit:
                result += tx.amount
            case .debit:
                result -= tx.amount
            case .transfer:
                if tx.account.id == account.id {
                    result -= tx.amount
                } else if tx.toAccount?.id == account.id {
                    result += tx.amount
                }
            }
        }
        return result
    }

    func runningBalance(for account: Account, transactions: [Transaction]) -> [BalanceDataPoint] {
        guard !transactions.isEmpty else { return [] }
        let sorted = transactions.sorted { $0.date < $1.date }
        var points: [BalanceDataPoint] = []
        var running = account.openingBalance
        points.append(BalanceDataPoint(date: sorted[0].date, balance: running))
        for tx in sorted {
            switch tx.type {
            case .credit:
                running += tx.amount
            case .debit:
                running -= tx.amount
            case .transfer:
                if tx.account.id == account.id {
                    running -= tx.amount
                } else if tx.toAccount?.id == account.id {
                    running += tx.amount
                }
            }
            points.append(BalanceDataPoint(date: tx.date, balance: running))
        }
        return points
    }
}
```

### Verify build

```bash
xcodebuild build \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Expected: `** BUILD SUCCEEDED **`

---

## Task 2 — Unit tests for `BalanceService.runningBalance`

**File:** `FinanceTrackerTests/Services/BalanceServiceTests.swift`

Append the following tests to the existing `BalanceServiceTests` suite (after the closing brace of `transferReducesSourceAndIncreasesDestination`, before the final `}`):

```swift
    @Test func runningBalanceReturnsEmptyForNoTransactions() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 500)
        ctx.insert(account)

        let result = BalanceService().runningBalance(for: account, transactions: [])
        #expect(result.isEmpty)
    }

    @Test func runningBalanceFirstPointIsOpeningBalance() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 1000)
        ctx.insert(account)
        let tx = Transaction(date: .now, amount: 200, payee: "Rent", type: .debit, account: account)
        ctx.insert(tx)

        let points = BalanceService().runningBalance(for: account, transactions: [tx])
        #expect(points.count == 2)
        #expect(points[0].balance == 1000)
    }

    @Test func runningBalanceAppliesDebitsCorrectly() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 1000)
        ctx.insert(account)
        let tx = Transaction(date: .now, amount: 300, payee: "Groceries", type: .debit, account: account)
        ctx.insert(tx)

        let points = BalanceService().runningBalance(for: account, transactions: [tx])
        #expect(points.last?.balance == 700)
    }

    @Test func runningBalanceAppliesCreditsCorrectly() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 0)
        ctx.insert(account)
        let tx = Transaction(date: .now, amount: 2000, payee: "Salary", type: .credit, account: account)
        ctx.insert(tx)

        let points = BalanceService().runningBalance(for: account, transactions: [tx])
        #expect(points.last?.balance == 2000)
    }

    @Test func runningBalanceSortsTransactionsByDate() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 1000)
        ctx.insert(account)
        let later = Calendar.current.date(from: DateComponents(year: 2026, month: 5, day: 20))!
        let earlier = Calendar.current.date(from: DateComponents(year: 2026, month: 5, day: 10))!
        let tx1 = Transaction(date: later,   amount: 200, payee: "Later",   type: .debit, account: account)
        let tx2 = Transaction(date: earlier, amount: 100, payee: "Earlier", type: .debit, account: account)
        ctx.insert(tx1); ctx.insert(tx2)

        let points = BalanceService().runningBalance(for: account, transactions: [tx1, tx2])
        // First point anchors at opening balance; second = 1000-100 = 900; third = 900-200 = 700
        #expect(points[0].balance == 1000)
        #expect(points[1].balance == 900)
        #expect(points[2].balance == 700)
    }
```

### Run new tests

```bash
xcodebuild test \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:FinanceTrackerTests/BalanceServiceTests \
  2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED"
```

Expected: all 9 tests pass (`TEST SUCCEEDED`).

### Commit

```
feat: add BalanceDataPoint and runningBalance(for:transactions:) to BalanceService
```

---

## Task 3 — Add `MonthlySpendingPoint` and `totalSpent` to `BudgetCalculationService`

**File:** `FinanceTracker/Services/BudgetCalculationService.swift`

Add a new value type and method. The file already has `BudgetProgress` and `progress(budget:transactions:)`. Append below the existing struct:

```swift
struct MonthlySpendingPoint: Identifiable {
    let id: UUID
    let month: Date
    let spent: Decimal

    init(month: Date, spent: Decimal) {
        self.id = UUID()
        self.month = month
        self.spent = spent
    }
}

extension BudgetCalculationService {
    func totalSpent(transactions: [Transaction]) -> Decimal {
        transactions
            .filter { $0.type == .debit }
            .reduce(Decimal.zero) { $0 + $1.amount }
    }
}
```

### Verify build

```bash
xcodebuild build \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Expected: `** BUILD SUCCEEDED **`

---

## Task 4 — Unit tests for `BudgetCalculationService.totalSpent`

**File:** `FinanceTrackerTests/Services/BudgetCalculationServiceTests.swift`

Append the following tests inside the `BudgetCalculationServiceTests` suite (before the final `}`):

```swift
    @Test func totalSpentSumsDebitsOnly() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking)
        ctx.insert(account)
        let d1 = Transaction(date: .now, amount: 50, payee: "A", type: .debit, account: account)
        let d2 = Transaction(date: .now, amount: 30, payee: "B", type: .debit, account: account)
        ctx.insert(d1); ctx.insert(d2)

        let result = BudgetCalculationService().totalSpent(transactions: [d1, d2])
        #expect(result == 80)
    }

    @Test func totalSpentIgnoresCreditAndTransfer() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking)
        ctx.insert(account)
        let debit    = Transaction(date: .now, amount: 40, payee: "Shop",  type: .debit,    account: account)
        let credit   = Transaction(date: .now, amount: 100, payee: "Pay",  type: .credit,   account: account)
        let transfer = Transaction(date: .now, amount: 200, payee: "Move", type: .transfer, account: account)
        ctx.insert(debit); ctx.insert(credit); ctx.insert(transfer)

        let result = BudgetCalculationService().totalSpent(transactions: [debit, credit, transfer])
        #expect(result == 40)
    }

    @Test func totalSpentReturnsZeroForEmptyInput() {
        let result = BudgetCalculationService().totalSpent(transactions: [])
        #expect(result == Decimal.zero)
    }
```

### Run new tests

```bash
xcodebuild test \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:FinanceTrackerTests/BudgetCalculationServiceTests \
  2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED"
```

Expected: all 6 tests pass.

### Commit

```
feat: add MonthlySpendingPoint and totalSpent(transactions:) to BudgetCalculationService
```

---

## Task 5 — Add `runningBalanceData(for:)` to `AccountViewModel`

**File:** `FinanceTracker/ViewModels/AccountViewModel.swift`

Add one computed method after `transactions(for:)`. The method calls `transactionRepo.fetch(for:)` then `balanceService.runningBalance(for:transactions:)`, and maps `Decimal` → `Double` at this layer.

Insert after the `transactions(for:)` method (after line 40 in current file):

```swift
    func runningBalanceData(for account: Account) -> [(date: Date, balance: Double)] {
        let txs = (try? transactionRepo.fetch(for: account)) ?? []
        return balanceService.runningBalance(for: account, transactions: txs)
            .map { (date: $0.date, balance: NSDecimalNumber(decimal: $0.balance).doubleValue) }
    }
```

### Verify build

```bash
xcodebuild build \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Expected: `** BUILD SUCCEEDED **`

---

## Task 6 — Unit tests for `AccountViewModel.runningBalanceData`

**File:** `FinanceTrackerTests/ViewModels/AccountViewModelTests.swift`

Append these tests inside `AccountViewModelTests` (before the final `}`):

```swift
    @Test func runningBalanceDataReturnsEmptyWhenNoTransactions() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 500)
        ctx.insert(account)
        try ctx.save()

        let vm = AccountViewModel(
            accountRepo: SwiftDataAccountRepository(context: ctx),
            transactionRepo: SwiftDataTransactionRepository(context: ctx)
        )

        #expect(vm.runningBalanceData(for: account).isEmpty)
    }

    @Test func runningBalanceDataConvertsDecimalToDouble() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let account = Account(name: "Checking", type: .checking, openingBalance: 1000)
        ctx.insert(account)
        let tx = Transaction(date: .now, amount: 250, payee: "Rent", type: .debit, account: account)
        ctx.insert(tx)
        try ctx.save()

        let vm = AccountViewModel(
            accountRepo: SwiftDataAccountRepository(context: ctx),
            transactionRepo: SwiftDataTransactionRepository(context: ctx)
        )
        let points = vm.runningBalanceData(for: account)

        #expect(points.count == 2)
        #expect(points[0].balance == 1000.0)
        #expect(points[1].balance == 750.0)
    }
```

### Run new tests

```bash
xcodebuild test \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:FinanceTrackerTests/AccountViewModelTests \
  2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED"
```

Expected: all 6 tests pass.

### Commit

```
feat: add runningBalanceData(for:) to AccountViewModel
```

---

## Task 7 — Add `spendingHistoryMonths`, `monthlySpendingHistory(for:)` to `BudgetViewModel`

**File:** `FinanceTracker/ViewModels/BudgetViewModel.swift`

Add the window constant and method. The method iterates over the past `spendingHistoryMonths` months relative to `selectedMonth`, fetches transactions per month via `transactionRepo.fetch(for:category:in:)`, and sums with `budgetCalcService.totalSpent(transactions:)`.

Add the following properties and method to the `BudgetViewModel` class body. Insert after the `unbudgetedCategories` computed property (before the closing `}`):

```swift
    static let defaultSpendingHistoryMonths = 12
    var spendingHistoryMonths: Int = BudgetViewModel.defaultSpendingHistoryMonths

    func monthlySpendingHistory(for category: Category) -> [(month: Date, spent: Double)] {
        let cal = Calendar.current
        let base = cal.date(from: cal.dateComponents([.year, .month], from: selectedMonth)) ?? selectedMonth
        return (0..<spendingHistoryMonths).reversed().compactMap { offset -> (month: Date, spent: Double)? in
            guard let month = cal.date(byAdding: .month, value: -offset, to: base) else { return nil }
            let txs = (try? transactionRepo.fetch(for: category, in: month)) ?? []
            let total = budgetCalcService.totalSpent(transactions: txs)
            return (month: month, spent: NSDecimalNumber(decimal: total).doubleValue)
        }
    }
```

### Verify build

```bash
xcodebuild build \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Expected: `** BUILD SUCCEEDED **`

---

## Task 8 — Unit tests for `BudgetViewModel.monthlySpendingHistory`

**File:** `FinanceTrackerTests/ViewModels/BudgetViewModelTests.swift`

Append these tests inside `BudgetViewModelTests` (before the final `}`):

```swift
    @Test func monthlySpendingHistoryReturnsCorrectNumberOfMonths() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let category = Category(name: "Food", type: .expense)
        ctx.insert(category)
        try ctx.save()

        let vm = BudgetViewModel(
            budgetRepo: SwiftDataBudgetRepository(context: ctx),
            transactionRepo: SwiftDataTransactionRepository(context: ctx),
            categoryRepo: SwiftDataCategoryRepository(context: ctx)
        )
        vm.selectedMonth = startOfMay2026()

        let points = vm.monthlySpendingHistory(for: category)
        #expect(points.count == BudgetViewModel.defaultSpendingHistoryMonths)
    }

    @Test func monthlySpendingHistoryRespectsCustomWindowSize() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let category = Category(name: "Food", type: .expense)
        ctx.insert(category)
        try ctx.save()

        let vm = BudgetViewModel(
            budgetRepo: SwiftDataBudgetRepository(context: ctx),
            transactionRepo: SwiftDataTransactionRepository(context: ctx),
            categoryRepo: SwiftDataCategoryRepository(context: ctx)
        )
        vm.selectedMonth = startOfMay2026()
        vm.spendingHistoryMonths = 3

        let points = vm.monthlySpendingHistory(for: category)
        #expect(points.count == 3)
    }

    @Test func monthlySpendingHistoryReturnsZeroForMonthsWithNoTransactions() throws {
        let container = try makeContainer()
        let ctx = ModelContext(container)
        let category = Category(name: "Food", type: .expense)
        ctx.insert(category)
        try ctx.save()

        let vm = BudgetViewModel(
            budgetRepo: SwiftDataBudgetRepository(context: ctx),
            transactionRepo: SwiftDataTransactionRepository(context: ctx),
            categoryRepo: SwiftDataCategoryRepository(context: ctx)
        )
        vm.selectedMonth = startOfMay2026()
        vm.spendingHistoryMonths = 3

        let points = vm.monthlySpendingHistory(for: category)
        #expect(points.allSatisfy { $0.spent == 0.0 })
    }
```

### Run new tests

```bash
xcodebuild test \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:FinanceTrackerTests/BudgetViewModelTests \
  2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED"
```

Expected: all 7 tests pass.

### Commit

```
feat: add spendingHistoryMonths and monthlySpendingHistory(for:) to BudgetViewModel
```

---

## Task 9 — Add Balance History section to `AccountDetailView`

**File:** `FinanceTracker/Views/Accounts/AccountDetailView.swift`

Add `import Charts` at the top, then insert a new `Section("Balance History")` inside the `List` — **before** the Transactions section. The section is hidden when there are no transactions.

Replace the entire file with:

```swift
import SwiftUI
import Charts

struct AccountDetailView: View {
    let account: Account
    @Bindable var viewModel: AccountViewModel

    var body: some View {
        let transactions = viewModel.transactions(for: account)
            .sorted { $0.date > $1.date }
        let balanceData = viewModel.runningBalanceData(for: account)

        List {
            Section {
                HStack {
                    Text("Balance")
                    Spacer()
                    Text(viewModel.balance(for: account),
                         format: .currency(code: account.currency))
                    .bold()
                }
                HStack {
                    Text("Type")
                    Spacer()
                    Text(account.type.rawValue.capitalized)
                        .foregroundStyle(.secondary)
                }
                HStack {
                    Text("Currency")
                    Spacer()
                    Text(account.currency)
                        .foregroundStyle(.secondary)
                }
            }

            if !balanceData.isEmpty {
                Section("Balance History") {
                    Chart(balanceData, id: \.date) { point in
                        LineMark(
                            x: .value("Date", point.date),
                            y: .value("Balance", point.balance)
                        )
                        .foregroundStyle(Theme.Charts.balanceLine)
                        .lineStyle(StrokeStyle(lineWidth: Theme.Charts.lineStrokeWidth))
                        AreaMark(
                            x: .value("Date", point.date),
                            y: .value("Balance", point.balance)
                        )
                        .foregroundStyle(Theme.Charts.balanceAreaFill)
                    }
                    .frame(minHeight: Theme.Charts.minHeight)
                    .padding(.horizontal, Theme.Spacing.cardPadding)
                }
            }

            if transactions.isEmpty {
                Section("Transactions") {
                    Text("No transactions yet")
                        .foregroundStyle(.secondary)
                }
            } else {
                Section("Transactions") {
                    ForEach(transactions) { tx in
                        HStack {
                            VStack(alignment: .leading, spacing: Theme.Spacing.tight) {
                                Text(tx.payee)
                                Text(tx.date,
                                     format: .dateTime.month(.abbreviated).day().year())
                                    .font(Theme.Typography.rowSubtitle)
                                    .foregroundStyle(.secondary)
                            }
                            Spacer()
                            Text(tx.amount,
                                 format: .currency(code: account.currency))
                            .foregroundStyle(tx.type == .credit ? Theme.Colors.positive : .primary)
                        }
                    }
                }
            }
        }
        .navigationTitle(account.name)
        .toolbar {
            ToolbarItem(placement: .topBarTrailing) {
                Button("Archive") {
                    try? viewModel.archive(account)
                }
            }
        }
    }
}
```

### Verify build

```bash
xcodebuild build \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Expected: `** BUILD SUCCEEDED **`

### Commit

```
feat: add Balance History chart section to AccountDetailView
```

---

## Task 10 — Add Spending History section to `BudgetDetailView`

**File:** `FinanceTracker/Views/Budgets/BudgetDetailView.swift`

Add `import Charts` at the top, then insert a new `Section("Spending History")` **after** the Progress section. The section is hidden when all months have zero spending.

Replace the entire file with:

```swift
import SwiftUI
import Charts

struct BudgetDetailView: View {
    let budget: Budget
    let progress: BudgetProgress
    @Bindable var viewModel: BudgetViewModel

    var body: some View {
        let spendingData = viewModel.monthlySpendingHistory(for: budget.category)
        let hasSpending = spendingData.contains { $0.spent > 0 }

        List {
            Section("Progress") {
                HStack {
                    Text("Spent")
                    Spacer()
                    Text(progress.spent, format: .currency(code: "USD"))
                        .bold()
                        .foregroundStyle(progress.isOverBudget ? Theme.Colors.destructive : .primary)
                }
                HStack {
                    Text("Limit")
                    Spacer()
                    Text(progress.limit, format: .currency(code: "USD"))
                }
                HStack {
                    Text("Remaining")
                    Spacer()
                    Text(progress.remaining, format: .currency(code: "USD"))
                        .foregroundStyle(progress.remaining < 0 ? Theme.Colors.destructive : Theme.Colors.positive)
                }
                ProgressView(value: min(progress.percentUsed, 1.0))
                    .tint(progress.isOverBudget ? Theme.Colors.destructive : Theme.Colors.primaryInteractive)
                    .padding(.vertical, Theme.Spacing.compact)
            }

            if hasSpending {
                Section("Spending History") {
                    Chart(spendingData, id: \.month) { point in
                        BarMark(
                            x: .value("Month", point.month, unit: .month),
                            y: .value("Spent", point.spent)
                        )
                        .foregroundStyle(Theme.Charts.spendingBar)
                    }
                    .frame(minHeight: Theme.Charts.minHeight)
                    .padding(.horizontal, Theme.Spacing.cardPadding)
                }
            }

            Section("Category") {
                Label(budget.category.name, systemImage: budget.category.icon)
                LabeledContent("Type", value: budget.category.type.rawValue.capitalized)
            }
        }
        .navigationTitle(budget.category.name)
        .toolbar {
            ToolbarItem(placement: .topBarTrailing) {
                Button("Delete Budget", role: .destructive) {
                    try? viewModel.delete(budget)
                }
            }
        }
    }
}
```

### Verify build

```bash
xcodebuild build \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -configuration Debug \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED"
```

Expected: `** BUILD SUCCEEDED **`

### Commit

```
feat: add Spending History chart section to BudgetDetailView
```

---

## Task 11 — Full test suite green

Run the complete test suite and verify nothing regressed:

```bash
xcodebuild test \
  -project FinanceTracker.xcodeproj \
  -scheme FinanceTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED"
```

Expected: `** TEST SUCCEEDED **`

If any test fails, fix the failure before proceeding to Task 12.

---

## Task 12 — Open PR

```bash
gh pr create \
  --base develop \
  --title "feat: charts — running balance line chart and monthly spending bar chart" \
  --body "$(cat <<'EOF'
## Summary
- Adds `BalanceDataPoint` and `runningBalance(for:transactions:)` to `BalanceService`
- Adds `MonthlySpendingPoint` and `totalSpent(transactions:)` to `BudgetCalculationService`
- Adds `runningBalanceData(for:)` to `AccountViewModel` (Decimal → Double conversion at VM layer)
- Adds `spendingHistoryMonths` (default 12, settable) and `monthlySpendingHistory(for:)` to `BudgetViewModel`
- Embeds a Swift Charts `LineMark`+`AreaMark` section in `AccountDetailView` (hidden when no transactions)
- Embeds a Swift Charts `BarMark` section in `BudgetDetailView` (hidden when all months zero)
- All chart tokens from `Theme.Charts` — no magic numbers in Views

## Test plan
- [ ] `BalanceServiceTests` — 5 new tests for `runningBalance`
- [ ] `BudgetCalculationServiceTests` — 3 new tests for `totalSpent`
- [ ] `AccountViewModelTests` — 2 new tests for `runningBalanceData`
- [ ] `BudgetViewModelTests` — 3 new tests for `monthlySpendingHistory`
- [ ] Full test suite green (`TEST SUCCEEDED`)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```
